# Backend API URL
# For local development — leave as empty string (falls back to window.location.origin)
# VITE_API_URL=http://localhost:8000

# For production — set this to your Render backend URL after deploying
# VITE_API_URL=https://herbvision-backend.onrender.com
